name: Audit Neon Storage V2

on:
  push:
    branches:
      - catalog-v2
    paths:
      - '.github/workflows/audit-neon-storage-v2.yml'
      - 'backend/app/scripts/audit_database_storage_v2.py'
  workflow_dispatch:

# Read-only final storage checkpoint after Yu-Gi-Oh Search V2 certification work.
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      DATABASE_URL_UNPOOLED: ${{ secrets.DATABASE_URL_UNPOOLED }}
    steps:
      - name: Checkout catalog-v2
        uses: actions/checkout@v4
        with:
          ref: catalog-v2
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install backend dependencies
        working-directory: backend
        run: pip install -r requirements.txt
      - name: Verify Neon secret
        run: test -n "${DATABASE_URL}${DATABASE_URL_UNPOOLED}"
      - name: Run read-only storage audit
        working-directory: backend
        run: |
          set -euo pipefail
          python -u -m app.scripts.audit_database_storage_v2 \
            --report-path /tmp/neon-storage-v2.json \
            | tee /tmp/neon-storage-v2.log
      - name: Upload storage evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: neon-storage-v2-${{ github.sha }}
          path: |
            /tmp/neon-storage-v2.json
            /tmp/neon-storage-v2.log
          retention-days: 90
          if-no-files-found: warn
