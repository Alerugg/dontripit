name: Legacy Pokemon Cardmarket Daily (Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "The Pokemon-only daily schedule is disabled to prevent duplicate partial refreshes."
          echo "Production scheduling is owned by daily-catalog-v2-orchestrator.yml."
