name: Legacy Ingest (Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Legacy daily_refresh scheduling is disabled."
          echo "Production scheduling is owned by daily-catalog-v2-orchestrator.yml and executes catalog-v2 certified jobs."
